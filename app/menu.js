import { Text, View } from "react-native";

export default function Menu() {
  return (
    <View style={{ padding: 24 }}>
      <Text>This is /menu — you should never see /m/enu in the address bar.</Text>
    </View>
  );
}
